# Copy to .env and adjust for your environment.

# IBVAP API gateway base URL (nginx, see ibvap/docker-compose.yml).
VITE_API_BASE_URL=http://127.0.0.1:8080/api/v1

# Realtime Gateway WebSocket URL (same gateway, different path/scheme).
VITE_WS_URL=ws://127.0.0.1:8080/ws
