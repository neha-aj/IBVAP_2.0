import StatusDot from '../common/StatusDot'; import Badge from '../common/Badge';
export default function CameraStatus({cameras}){return <section className="panel"><div className="border-b px-4 py-3"><h2 className="font-semibold">Camera Status</h2></div><div className="divide-y">{cameras.slice(0,4).map(c=><div className="flex items-center justify-between px-4 py-3" key={c.id}><div className="flex items-center gap-3"><StatusDot status={c.status}/><div><p className="font-medium">{c.name}</p><p className="text-xs text-muted">{c.location}</p></div></div><Badge tone={c.status}>{c.status}</Badge></div>)}</div></section>}
