export default function DetectionOverlay({detections=[]}){return <div className="pointer-events-none absolute inset-0">{detections.map(d=>{const box=d.bbox||d;return <div key={d.id} className={`absolute border ${d.type==='person'?'border-info':'border-warning'}`} style={{left:`${box.x}%`,top:`${box.y}%`,width:`${box.width}%`,height:`${box.height}%`}}><span className={`absolute -top-5 left-0 whitespace-nowrap px-1 py-0.5 text-[9px] font-bold tracking-wide ${d.type==='person'?'bg-info text-primary':'bg-warning text-ink'}`}>{d.type.toUpperCase()} {Math.round(d.confidence*100)}%{d.trackId?` · #${d.trackId}`:''}</span></div>})}</div>}
