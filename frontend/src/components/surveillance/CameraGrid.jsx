import CameraCard from './CameraCard'; export default function CameraGrid({cameras,detections,posesByCamera={},dailyCounts={},selectedCamera,onSelect,view='grid',pausedIds,onTogglePause}){return <div className={view==='grid'?'grid gap-4 sm:grid-cols-2 2xl:grid-cols-3':'grid gap-3'}>{cameras.map(c=><CameraCard key={c.id} camera={c} detections={detections.filter(d=>d.cameraId===c.id)} poses={posesByCamera[c.id]||[]} dailyCounts={dailyCounts[c.id]} selected={selectedCamera?.id===c.id} onSelect={onSelect} paused={pausedIds?.has(c.id)||false} onTogglePause={onTogglePause}/>)}</div>}
