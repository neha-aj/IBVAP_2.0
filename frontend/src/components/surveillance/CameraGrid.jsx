import CameraCard from './CameraCard'; export default function CameraGrid({cameras,detections,dailyCounts={},selectedCamera,onSelect,view='grid'}){return <div className={view==='grid'?'grid gap-4 sm:grid-cols-2 2xl:grid-cols-3':'grid gap-3'}>{cameras.map(c=><CameraCard key={c.id} camera={c} detections={detections.filter(d=>d.cameraId===c.id)} dailyCounts={dailyCounts[c.id]} selected={selectedCamera?.id===c.id} onSelect={onSelect}/>)}</div>}
