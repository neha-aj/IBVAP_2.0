import Badge from '../common/Badge';
export default function CameraDetails({camera}){const rows=[['Camera ID',camera.id],['Location',camera.location],['Status',<Badge tone={camera.status}>{camera.status}</Badge>],['Resolution',camera.resolution],['Frame Rate',`${camera.fps} FPS`],['Connection',camera.status==='offline'?'Disconnected':'Stable'],['Last Updated',camera.status==='offline'?camera.lastActive:'Just now']];return <section className="panel p-4"><p className="eyebrow">Camera Information</p><dl className="mt-3 space-y-2.5 text-[12px]">{rows.map(([label,value])=><div className="grid grid-cols-[105px_1fr] gap-3" key={label}><dt className="text-muted">{label}</dt><dd className="min-w-0 text-secondary">{value}</dd></div>)}</dl></section>}
