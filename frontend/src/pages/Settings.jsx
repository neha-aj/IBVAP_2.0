import PageHeader from '../components/layout/PageHeader'; import Badge from '../components/common/Badge';
const groups=[['System',['System status','Deployment mode','Server status']],['Detection',['Human Detection','Vehicle Detection','ANPR','Face Detection','Intrusion Detection','Night Detection']],['Alerts',['Alert notifications','Alert severity threshold','Event retention']],['Camera',['Camera configuration','Stream configuration']]];
export default function Settings(){return <><PageHeader title="System Settings" subtitle="Static configuration surface for future system integration"/><div className="grid gap-5 lg:grid-cols-2">{groups.map(([title,items])=><section className="panel" key={title}><div className="border-b px-4 py-3"><h2 className="font-semibold">{title}</h2></div><div className="divide-y">{items.map((item,i)=><div className="flex items-center justify-between px-4 py-3" key={item}><div><p>{item}</p><p className="mt-0.5 text-xs text-muted">Configuration pending backend connection</p></div>{title==='System'?<Badge tone={i===2?'online':'neutral'}>{i===0?'operational':i===1?'edge deployment':'available'}</Badge>:<div className="h-5 w-9 rounded-full bg-slate-700 p-0.5"><div className="h-4 w-4 rounded-full bg-slate-400"/></div>}</div>)}</div></section>)}</div></>}
